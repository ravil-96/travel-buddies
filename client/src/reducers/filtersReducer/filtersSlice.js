const initialState = {
    filters: {
        date: ['oldest', 'latest']
        }
}

const filtersReducer = (state=initialState, action) => {
    switch(action.type) {
        case 'filters/dateFilterSelected’':{
            //modify slice state here
        }
    }
}